I produced this jointly with Sean Murray and Ashwin Dandekar.

Our part of the project was implementing the processor.vhd file and its componenets which functionally represents the datapath of the processor.  The skeleton.vhd is the top level component used to test our processor and provide an interface with the keyboard (this was provided to us). 